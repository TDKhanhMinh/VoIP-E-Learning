# File: VOIP E-LEARNING/docker-compose.yml
version: "3.8"

services:
  # AWS S3 recording is currently unavailable, so its Redis queue is disabled.
  # redis:
  #   image: "redis:7-alpine"
  #   container_name: lms-redis
  #   ports:
  #     - "6379:6379"
  #   volumes:
  #     - redis_data:/data
  #   restart: always

  # 2. Dịch vụ API Server (Producer & Webhook Handler)
  api:
    # Build image từ thư mục backend
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: lms-api
    # Lệnh chạy Express Server của bạn
    command: npm run start
    ports:
      - "5000:5000" # Mở cổng API
    env_file:
      - ./backend/.env
    # AWS S3 recording and its BullMQ queue are currently unavailable.
    # environment:
    #   REDIS_HOST: redis
    # depends_on:
    #   - redis
    restart: always

  # AWS S3 recording and its AI worker are currently unavailable.
  # worker:
  #   build:
  #     context: ./backend
  #     dockerfile: Dockerfile
  #   container_name: lms-worker
  #   command: node src/service/aiWorkerService.js
  #   env_file:
  #     - ./backend/.env
  #   environment:
  #     REDIS_HOST: redis
  #   depends_on:
  #     - redis
  #   restart: always

# AWS S3 recording is currently unavailable.
# volumes:
#   redis_data:
#     driver: local
    
