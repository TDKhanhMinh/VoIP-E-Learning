# Install under /etc/logrotate.d/backend-remake after reviewing the deployment
# account, directory ownership and durable volume mount.
/var/log/voip-elearning/*.log {
  daily
  maxsize 10M
  rotate 30
  maxage 30
  missingok
  notifempty
  compress
  delaycompress
  copytruncate
}
